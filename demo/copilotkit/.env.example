# LLM API key (Anthropic). Required for the CopilotKit runtime to talk to a model.
ANTHROPIC_API_KEY=sk-ant-...

# Optional: override the MCP server URL.
# Defaults to http://localhost:3001/mcp (the /demo server in this repo).
# MCP_SERVER_URL=http://localhost:3001/mcp
